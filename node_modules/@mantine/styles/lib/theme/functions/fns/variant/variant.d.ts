import type { MantineColor, MantineGradient, MantineThemeBase } from '../../../types';
export interface VariantInput {
    variant: 'filled' | 'light' | 'outline' | 'default' | 'gradient' | 'white' | 'subtle';
    color?: MantineColor;
    gradient?: MantineGradient;
}
export interface VariantOutput {
    border: string;
    background: string;
    color: string;
    hover: string;
}
export declare function variant(theme: MantineThemeBase): ({ variant, color, gradient }: VariantInput) => VariantOutput;
//# sourceMappingURL=variant.d.ts.map